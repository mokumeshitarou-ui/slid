# コンテンツ制作 AIエージェントチーム

## プロジェクト概要

このプロジェクトは、複数のAIエージェントが役割分担してコンテンツを制作する協力型チームです。
すべてのやり取りは**日本語**で行います。

## チーム構成

| エージェント | 役割 | 担当フェーズ |
|---|---|---|
| リサーチャー | 情報収集・調査 | 1. 企画・調査 |
| ライター | 執筆・構成 | 2. 執筆 |
| エディター | 編集・校正 | 3. 編集・校正 |
| ディレクター | 全体統括・品質管理 | 全フェーズ |

## ワークフロー

1. **企画** → ディレクターがテーマ・方針を決定
2. **調査** → リサーチャーが情報を収集・整理
3. **執筆** → ライターが原稿を作成
4. **編集** → エディターが校正・改善
5. **最終確認** → ディレクターが品質チェック・承認

## エージェント呼び出し方法

各エージェントは以下のように呼び出せます：

- `@researcher` または「リサーチャーとして調査して」→ 調査タスクを実行
- `@writer` または「ライターとして書いて」→ 執筆タスクを実行
- `@editor` または「エディターとして校正して」→ 編集タスクを実行
- `@director` または「ディレクターとして確認して」→ 統括・品質管理を実行

## 作業ルール

- 出力はすべて日本語で行う
- 各エージェントは `agents/` ディレクトリ内の自身の定義に従って行動する
- 成果物は `output/` ディレクトリに保存する
- テンプレートは `templates/` ディレクトリを参照する
- 作業途中の下書きは `drafts/` ディレクトリに保存する

## ディレクトリ構成

```
slide/
├── CLAUDE.md              # このファイル（チーム設定）
├── agents/                # エージェント定義
│   ├── researcher.md      # リサーチャーの役割定義
│   ├── writer.md          # ライターの役割定義
│   ├── editor.md          # エディターの役割定義
│   └── director.md        # ディレクターの役割定義
├── templates/             # コンテンツテンプレート
│   ├── blog-post.md       # ブログ記事テンプレート
│   ├── sns-post.md        # SNS投稿テンプレート
│   └── report.md          # レポートテンプレート
├── workflow/              # ワークフロー定義
│   └── content-creation.md # コンテンツ制作フロー
├── drafts/                # 作業中の下書き
└── output/                # 完成した成果物
```
